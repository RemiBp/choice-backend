export enum TransactionType {
    PURCHASE = "purchase",
    RENEWAL = "renewal",
    REFUND = "refund",
    UPGRADE = "upgrade",
    DOWNGRADE = "downgrade",
}

export enum TransactionStatus {
    SUCCESS = "success",
    FAILED = "failed",
    PENDING = "pending",
}
